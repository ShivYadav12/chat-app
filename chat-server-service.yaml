apiVersion: v1
kind: Service
metadata:
  name: chat-server-service
spec:
  type: NodePort
  selector:
    app: chat-server
  ports:
  - protocol: TCP
    port: 54000
    targetPort: 54000
    nodePort: 30000

